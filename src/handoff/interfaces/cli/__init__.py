"""Future CLI interface, parallel to the Streamlit UI."""

from __future__ import annotations

__all__ = ["run_cli"]


def run_cli() -> None:
    """Entrypoint for future handoff CLI subcommand. Stub for now."""
    raise NotImplementedError(
        "The handoff CLI is not yet implemented. "
        "Please use the Streamlit UI entrypoint for now."
    )
