## Release 2026.3.6 Plan

* Rename `run.bat` to `handoff.bat`
* Add version number to the folder name when build full
* Turn `build-full` and `build-patch` commands into `build --full` and `build 
--patch`
* Rename `bump-version` command to just `bump`
* Add a `Send log` button in Settings page, which zips the log file and downloads
 it. (This way the user won't ever need to touch where logs and db are stored).
* Sync the project descriptions in README.md to the About section in Settings.
* Add better test coverage with meaningful tests.


